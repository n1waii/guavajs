import Event from "./event.js";
import * as PhysicsEngine from "../../physics.js";

export class TouchedEvent extends Event {
    Connect(callback) {
        super.Connect(callback);
        PhysicsEngine.
    }
};